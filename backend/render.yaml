# Render Blueprint — Infrastructure as Code
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: bluefinance-api
    env: docker
    repo: https://github.com/your-org/BlueFinance.git
    branch: main
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: PORT
        value: "8080"
      # CORS: SetIsOriginAllowed(_ => true) cho phép mọi origin
      # Supabase PostgreSQL connection string (pooled)
      - key: ConnectionStrings__DefaultConnection
        value: "Host=aws-1-ap-southeast-1.pooler.supabase.com;Port=6543;Database=postgres;Username=postgres.speqmdsioarzccxwxvci;Password=Nguyenmylb85@!;SSL Mode=Require;Trust Server Certificate=true;Command Timeout=120;Pooling=false"
    healthCheckPath: /api/wallets
    autoDeploy: true
